# /etc/house-power/backend.env — lives ON THE PI ONLY, never in git.
# DB_PASSWORD is generated by pi-hosted/install.sh on first install.
DB_DSN=postgresql://house_power:CHANGE_ME@localhost:5432/house_power
MQTT_HOST=localhost
ASSUMED_VOLTS=120
ENABLE_NILM=false
